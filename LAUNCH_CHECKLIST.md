# Launch checklist
Verify PostgreSQL connectivity at `/healthz`, Stripe webhook configuration, active membership enforcement, server-side admin permissions, job budget validation, and mutual ratings before production deployment.
